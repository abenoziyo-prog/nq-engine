# Secrets — real credentials never commit (template is .env.example)
.env
.env.*
!.env.example

# Python
__pycache__/
*.pyc

# Virtualenv — never commit the interpreter/site-packages
.venv/

# Working scratch data dir (canonical CSVs live in src/data/)
data/
