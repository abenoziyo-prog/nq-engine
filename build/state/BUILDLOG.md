# Build Log

Append one entry per completed task: timestamp, task id, acceptance result, commit hash.


## 2026-06-13 02:31 — T01 backtest-harness DONE
Acceptance PASS: src/backtest/harness.py reproduces EMA_PROX_V4_5M base config exactly (n=21, +4935pt, PF 5.66). Regression baseline locked. Next workable: T02 (regression suite), T03 (data store), T04 (risk tests), T07 (drift) — all unblocked.

## 2026-06-12 — T04 risk-manager-tests DONE
Acceptance PASS: tests/test_risk.py — 18 boundary cases, all green. Covers every limit in the spec against src/risk/manager.py: per-trade $ ceiling (exact numbers: 98pt stop @ $2 = $196/ct < $200 → ALLOW 1; 2-lot tier $392 → DOWNSIZE to 1; 40pt $160 → ALLOW 2), catastrophe-too-wide (100pt $200 boundary ALLOW, 100.5pt $201 REJECT, invalid/<=0 stop REJECT), trailing-DD proximity HALT (room <= $200 boundary), session-loss HALT (<= -$600 boundary), daily-align +1 only with $400 buffer AND alignment, max-contracts clamp, FLAT always ALLOW even when halted / DD-breached / session-stopped. Manager required no code changes — already correct at every boundary. Regression guard tests/test_core.py still PASS. Next workable in P1: T05 walkforward, T06 factorial, T07 drift (all dep T01, DONE).

## 2026-06-15 — T07 drift-control DONE
Acceptance PASS: src/backtest/drift_control.py reproduces the recorded ~0.93 NY-morning-long MFE/MAE drift benchmark (strategy_vault.json OB_STRICT_SINGLE_TOUCH "drift control 0.93"). Random same-session NY_AM long entries, fixed horizon, floor-at-0 excursion ratio = Σmfe/Σmae: single-seed 0.929 (H=6, n=500, seed=12345); Monte-Carlo pooled 0.928 / mean 0.934 over 200 seeds (p5/50/95 = 0.78/0.93/1.09). Deterministic via local random.Random(seed) — never the global RNG; MC seed list derived deterministically from cfg.seed → fully re-runnable. Reuses harness Stats/_stats (no stat-math duplication) and Session-tagged Bars. CLI: `python -m src.backtest.drift_control [--mc N]`. Buy-and-hold NOT implemented (retired per operator directive). tests/test_drift.py 4 cases green; regression guards tests/test_core.py + tests/test_risk.py still PASS. Next workable in P1: T05 walkforward, T06 factorial (both dep T01, DONE).

## 2026-06-15 — T02 engine-regression-suite DONE
Acceptance PASS: tests/test_vault_regression.py — 5 cases green, the engine guard rail. Each assertion names the vault id + tolerance (n exact; win±0.5, total±5pt, PF±0.05, maxDD±5). Locked: EMA_PROX_V4_5M base (n=21, +4934.75, PF 5.66, maxDD -736.5 — matches T01 + vault exit_study "+4,935"); daily-align variant via reconstructed prior-day daily 9x50 gap (n=21, +8113.75, PF 6.96 — inside vault rules.sizing "PF 5.7->8.1" uplift band, and asserted > base); stop-4ATR variant (n=25, +2827.79, PF 3.88, maxDD -305 — disaster-stop path); EMA_PROX_V0B_5M EXACT vault headline (n=40, win 45.0%, +4380.25, PF 3.11, maxDD -1248.5). 15m V4 configs reported PENDING (baselines not calibrated), not failed. All prior suites (test_core/test_risk/test_drift) still PASS.
INTEGRITY FLAGS to operator (did NOT touch vault): (1) V4_5M results-block (n=22/total5461/PF16.01/maxDD-212) is STALE vs the T01 re-baseline (n=21) — not reproducible by the current harness; locked harness output rather than fabricate a match. (2) Sharpe convention mismatch (V0B vault sharpe 3.81 vs harness 5.87; V4 base vault 5.0 vs harness 8.74) — Sharpe excluded from all assertions. (3) data/MNQ_15m_aggregated_clean.csv is actually PRESENT in the repo (contrary to the task note) — 15m configs are skippable only because their baselines aren't calibrated yet, not because the file is missing. Next workable: T03 data-store, T05 walkforward, T06 factorial; T08/T09 now unblocked (dep T02 DONE).

## 2026-06-15 — T02 path correction (data CSVs -> src/data/)
Followup on T02 after the upstream merge (bd251d9) added the canonical bar store under src/data/. Repointed tests/test_vault_regression.py at src/data/MNQ_5m/15m_aggregated_clean.csv via a repo-root-relative absolute path (CWD-independent). Reason: the top-level data/ directory is UNTRACKED (git ls-files data/ is empty) while src/data/*.csv IS committed — the original data/ path would have failed on a fresh clone. All 5 regression cases still green with identical locked numbers (src/data content == data/ content), verified both from repo root and from /tmp; test_core/test_risk/test_drift still pass. NOTE: src/backtest/drift_control.py DEFAULT_DATA still references the untracked data/ — left untouched (T07 scope, not T02); flagged for a future fix.

## 2026-06-15 — Research sync: 1m databento + OB NY-open strategy + V4 OOS validation
Integrated externally-produced research into the repo (analysis NOT re-run this session; results recorded with provenance).
DATA: confirmed src/data/MNQ_1m_12mo_databento.csv present + readable — 352,695 1m bars, cols [time,ts_utc,ts_et,open,high,low,close,volume], 2025-06-15..2026-06-14, integrity-verified earlier this session (0 dupes, 0 OHLC violations). Now the primary dataset.
VAULT: added 3 entries (strategies now 18): OB_NYOPEN_BULL_1M (CANDIDATE — n=798, PF 2.26, +$15,968, maxDD -$323 fits $2K prop, 13/13 green, beats drift 2.26 vs 0.90); OB_NYOPEN_VOLUME_CONFIRM (FALSIFIED — volume-confirming the OB displacement leg reduced edge; no volume filter in OB engines); OB_NYOPEN_MACRO_WINDOW (FALSIFIED — 09:50 macro window weakest; 09:30-09:50 open window best). All 3 tagged provenance=operator-external, UNVERIFIED in-session, pending in-repo regression (T24). JSON validated.
DISCREPANCY FLAGGED: these 3 entries were described as 'added externally' but were NOT present in the vault and there was no external merge in git history (last commit was the prior session's a21e6d8). They were CREATED here from operator-provided numbers, not confirmed pre-existing. Recorded honestly rather than fabricating an external-merge provenance.
V4 OOS in STRATEGY_VAULT.md: V4_long_5m OOS PF 2.39 (vs in-sample 5.66/16.0), maxDD -$2,582 EXCEEDS the $2K prop limit -> not prop-deployable as-is; V4_long_15m OOS PF 3.67, maxDD -$1,080 -> fits the limit (prop-viable EMA_PROX timeframe). IS-vs-OOS now labeled explicitly; added findings #9 (deployment/DD-vs-prop-limit is the ranking criterion) and #10 (volume-confirm filters keep failing). $-convention note added (legacy rows NQ $20/pt; new databento rows MNQ $2/pt prop dollars).
BUILD TASKS (spec'd PENDING, NOT built): T24 ob-nyopen-engine [T01], T25 ob-walkforward-oos [T24,T05], T26 shock-v1-build [T01] — in build/tasks/specs_P6_research.md, STATUS.json, and the manifest P6 block.
Regression guards (core/risk/drift/vault) all still green.

## 2026-06-15 — T24 ob-nyopen-engine BLOCKED (in-repo verification did not reproduce)
Implemented src/engine/ob_nyopen.py exactly per the OB_NYOPEN_BULL_1M spec (OB = last down candle before a >=1.5*ATR(14) body up-displacement breaking the prior 10-bar high, formed 09:30-11:00 ET; enter long at first same-day tap of the zone top; stop 1pt below zone low; target entry+2*max(zone_height,3); 120-min max hold; 1pt friction). Time-aware engine driven through the unmodified harness via the daily_gap_fn(ts) hook (feed_ts) — regression suite untouched.
RESULT: MISMATCH. In-repo n=164, PF 0.94, total -150.5pt, maxDD -499pt, 7/13 green (a net LOSER) vs operator-external n=798, PF 2.26, +7984pt, maxDD -162pt, 13/13 green. Not a near-miss — opposite sign of edge.
DIAGNOSIS (read-only, no tuning): the ~5x entry-count gap cannot be closed by tuning. OB FORMATIONS in 09:30-11:00 number only 210 (body-displacement) or 551 (range-displacement) over 12 months — both < the 798 ENTRIES claimed externally. So the external pipeline uses a different setup definition (multi-bar displacement leg, multiple/re-entry taps per zone, concurrent positions, wider window, or different displacement/zone-edge metric) not specified in the vault description. Also flagged: the vault entry's rules.window ('09:30-09:50') disagrees with the T24 spec ('09:30-11:00').
ACTION: per operator directive, engine NOT adjusted to force a match. T24 set BLOCKED pending the exact setup definition. OB_NYOPEN_BULL_1M kept CANDIDATE but provenance/caveats now flag the failed in-repo verification with both number sets (in_repo_verification block). Regression guards (core/risk/drift/vault) all still green.

## 2026-06-15 — T24 RESOLVED → DONE (single-entry OB falsified; re-entry variant spec'd)
Operator confirmed the external n=798/PF2.26 was a multiple-re-entry artifact the single-entry vault spec did not capture — directed NOT to tune toward it. Resolution:
- OB_NYOPEN_BULL_1M (single-entry, as originally specified) -> FALSIFIED. Verified in-repo numbers are now the record: n=164, PF 0.94, total -150.5pt, maxDD -499, 7/13 green (net LOSER). External 798/2.26 retained only as `external_artifact_rejected` (do-not-use). Finding: "Single-entry NY-open bull OB has no edge as specified; external winning number was a re-entry artifact, not reproducible under one-entry-per-zone."
- NEW entry OB_NYOPEN_REENTRY_1M (SPEC_ONLY, results=null): precisely specifies the re-entry variant — single-bar >=1.5*ATR body displacement breaking prior 10-bar high; 09:30-11:00 ET formation; zone valid until a 1m CLOSE beyond its far edge; RE-ENTRY on each re-tap of a still-valid zone = new entry; stop 1pt below zone low, target 2*max(zone_height,3), 120min hold; single-position (skip overlapping taps and log the count). NOT backtested — future research task.
- T24 -> DONE: the verification correctly falsified the single-entry version (that was its job). Engine src/engine/ob_nyopen.py NOT tuned. Regression guards (core/risk/drift/vault) all still green.

## 2026-06-17 — T27 meanrev-fade-verify DONE; cross-family FINDING (operator-external sync)
NEW FAMILY verified. Implemented src/engine/meanrev_fade.py (2m: enter LONG when (close-EMA9)/ATR14 <= -3.0, exit when close >= EMA9-0.5*ATR, long-only, no stop, 1pt friction) and ran through the harness on 1m databento resampled to 2m (176,350 bars; runner research/task_t27_meanrev.py). Engine NOT tuned.
RECONCILIATION vs operator-external: FULL 12mo MATCHES near-exactly (in-repo n=191/PF5.06/maxDD-245.5/+8080pt vs external n=192/PF5.07/-246/+8112) -> engine faithful. BLIND earliest-6mo DIVERGES on magnitude (in-repo PF 5.27 n=103 vs claimed 6.98 n=111) -- a blind-window BOUNDARY difference, NOT a bug (the exact full-sample match rules out lookahead/re-entry-type errors). Blind still improves OOS (5.27>5.06 full). Session breakdown reproduces qualitatively: Asia 6.37 / London 6.23 strong, NY_AM 0.84 weak ('fade fails where NY trends'); NY_PM 22.37 (n=16) is small-n noise contradicting 'NY weak'.
STATUS: MEANREV_FADE_2M -> CANDIDATE (verified-in-repo). Blind clears the gate on its own: PF 5.27>1.5, beats drift 0.75, n=103 adequate, concentration 23%<60%, 0.52 trades/day, DD -245pt (-$491, fits $2K). Divergent blind-PF (6.98 vs 5.27) flagged for operator reconciliation, not tuned. Caveat: mean-rev long is regime-exposed (falling-knife), no stop, bull-year-only -> needs down-regime/2nd OOS + stop test before promotion.
CROSS FAMILY: added EMA_CROSS_CASCADE_RSI55 (FINDING, operator-external/UNVERIFIED) -- ~480 cross configs swept, best 3m>5m>15m 7x50 long +RSI55 PF floor 2.36 but only ~80 trades/yr; none hit PF>4 with adequate frequency. Superseded by the mean-rev fade for the frequency+PF target. Referenced cross_sweep_master_matrix.csv NOT present in repo. Does not overturn EMA_CROSS_CONFIRMED FALSIFIED.
NOTE: operator labeled the verify task 'T25' but T25 is already ob-walkforward-oos (now moot, OB falsified); used next free id T27 to avoid overwrite. Regression guards (core/risk/drift/vault) all green.

## 2026-06-17 — Aggregate portfolio paper model (PORTFOLIO_PAPER_V1)
Built src/research/portfolio.py: a single aggregate model combining the verified/OOS-surviving sleeves only — MEANREV_FADE_2M (2m), LVL_IMB_LONDON_5M (5m), LVL_IMB_ASIA_5M (5m), EMA_PROX_V4_15M (15m); equal weight, 1 contract/sleeve, long-only, 1pt friction. FALSIFIED/PARKED/OOS-failed/unverified-legacy entries EXCLUDED; SHOCK_V1 set aside (needs volume + own loop).
Aggregate characterization (12mo): FULL n=343 PF 4.26 +15,093pt maxDD -$995 top3-conc 26%; BLIND earliest-6mo (verdict) n=199 PF 3.27 +6,946pt maxDD -$995 top3-conc 31% (far below individual sleeves -> diversification is the main gain); RECENT 6mo PF 6.16. Max concurrent open positions = 3 (risk manager max_contracts=3 accommodates exactly). ~80% of the blind edge is the mean-rev fade; London/Asia/V4_15M are smaller diversifiers (Asia n=24 & V4_15M n=19 are fragile high-concentration passengers).
Forward paper-logging wired (logs/portfolio_paper.jsonl, idempotent, NO orders) + routine P-PORT (daily 17:30 ET). Currently prints "no forward data yet" (no post-2026-06-14 bars) -> the genuine OOS test is the paper log accruing over time. Recorded as PORTFOLIO_PAPER_V1 (CANDIDATE, paper-tracking; NOT promoted to capital). Constitution honored: research only, never places orders, sleeves not tuned. Regression guards (core/risk/drift/vault) all green.

## 2026-06-17 — Portfolio expanded to ALL runnable sleeves (operator: "add all, nothing to lose")
src/research/portfolio.py now wires EVERY strategy with a runnable engine as a PAPER sleeve (13 total), tiered CORE / DIVERSIFIER / WATCH / FALSIFIED-WATCH / SHOCK. Per-sleeve blind PF logged. Not-wireable (and why) documented: short (harness long-only), no-engine cross family, PARKED, DAY_MAP (context), OB ablations, V4_1M_TEST stub.
KEY FINDING: equal-weight-ALL DILUTES the edge -> blind PF 1.49, maxDD -$7,382 (WATCH/FALSIFIED sleeves are net drags), concurrency 9. CORE (fade + London) carries the edge -> blind PF 4.64, maxDD -$386, concurrency 2. So 'add everything' is for FORWARD DATA COLLECTION, not a deployable equal-weight blob; capital -> CORE only (unchanged). Side note: first in-repo blind read of 15m legacy configs (K15 3.21, V0_15M_K15 2.90) held up better than expected on small n -> observation-only.
Constitution honored: PAPER only, never places orders, FALSIFIED statuses unchanged (logged as 'confirm stays dead'), no sleeve tuned. Forward paper log + routine P-PORT updated for all sleeves. Still 'no forward data yet' until post-2026-06-14 bars arrive. PORTFOLIO_PAPER_V1 vault entry updated (ALL vs CORE results, per-sleeve blind PF, key finding). Regression guards all green.

## 2026-06-17 — T12+T13+T14 DONE: Tradovate demo bridge path (paper, auth stubbed)
Built the zero-cost paper-execution path the operator asked for ("load the model into a paper account").
- T12 src/bridge/tradovate_client.py: REST+WS client, auth STUBBED to .env (TRADOVATE_*). DRY_RUN by DEFAULT — records order payloads (paper log), returns simulated acks, performs NO network I/O, imports no network lib. DEMO_LIVE requires demo creds AND explicit opt-in, and even then auth/placement raise NotImplementedError pending T18 live-handshake validation. No path to a real-money order.
- T13 src/bridge/oso.py: isAutomated:true OSO bracket (entry + OCO stop/target), tick-rounded (0.25), geometry-validated (long: stop<entry<target; short mirror); build_flatten for closes.
- T14 src/bridge/bridge.py: the chokepoint — Signal -> RiskManager.evaluate (the $50K/$2K veto) -> OSO bracket -> client. ALLOW/DOWNSIZE place a paper OSO; REJECT/HALT are DROPPED with no order; FLAT always flattens (closing risk allowed even when halted). Reuses the T04 risk boundaries (98pt/$196 -> ALLOW 1; 120pt/$240 -> REJECT).
tests/test_bridge.py: 9 cases green (dry-run defaults, stays-dry-run-with-creds, DEMO_LIVE blocked, OSO geometry/tick/isAutomated, bridge allow/reject/halt/flat). All prior guards (core/risk/drift/vault) still green.
NEXT: free Tradovate DEMO signup -> drop keys in .env. T18 paper-integration-test stays BLOCKED_ON_KEYS (also needs T17 live-feed-adapter). The model's signals can now be routed through risk -> paper OSO with zero live risk.

## 2026-06-17 — Edgeful API client (read-only stats) + plan limits discovered
Built src/data/edgeful_client.py (stdlib urllib; Bearer auth; key from .env EDGEFUL_API_KEY, never hard-coded). Base https://api.edgeful.com; GET /report_calculation/{report}/{market_type}/{ticker}?start_date&end_date (75 report endpoints in their OpenAPI). Key VALID (authenticates). PLAN LIMIT found: futures ticker RTY only (NQ -> "upgrade to Pro/All Access", code ticker_not_allowed), and only 3 reports allowed (previous-days-range, opening-stats, green-and-red-days-by-weekday); gap-fill/ORB/etc. require upgrade. So the API gives little for our NQ model directly. Captured the 3 allowed reports' schema/definitions (e.g. previous-days-range: PDH-broken count/% then closed-green/red split; opening-stats: days above/between/below prior H-L) to match Edgeful methodology when reproducing on our own NQ data. SECURITY: key was pasted in chat -> recommended rotation; stored only in gitignored .env; .env.example documents the slot. WAF note: api.edgeful.com 403s default python-urllib UA; send a normal User-Agent. Edgeful API is a zero-cost cross-check at best (paid for NQ); the core quantification reproduces reports on our NQ data and backtests them — no key required.
